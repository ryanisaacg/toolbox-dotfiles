# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Ryan Goldstein
	email = ryanisaacg@gmail.com
[alias]
    ff = merge --ff-only
    down = !git fetch && git ff
    up = push

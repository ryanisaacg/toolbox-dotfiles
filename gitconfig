# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Ryan Goldstein
	email = ryanisaacg@gmail.com
[alias]
    ff = merge --ff-only
    down = !git fetch --prune && git ff
    up = push
    staged = diff --cached
    tags = tag -l
    branches = branch -a
    remotes = remote -v
[core]
	excludesfile = /Users/ryangoldstein/.gitignore_global
